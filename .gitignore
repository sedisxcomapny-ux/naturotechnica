# Environment
.env
.env.local
.env.production

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Data
# data/raw/ is intentionally NOT gitignored — the pilot CSVs ship with the
# image so Railway serves real data on first boot. User-mutable runtime
# state stays excluded.
data/processed/
data/naturotechnica.db
data/fields.json
data/settings.json

# ML models (binary artifacts — track with MLflow instead)
models/*.pkl
models/*.joblib
models/*.model

# Node / Frontend
node_modules/
.next/
out/
npm-debug.log*

# macOS
.DS_Store
.AppleDouble

# VS Code
.vscode/settings.json

# Logs
*.log
logs/

# MLflow
mlruns/

# Claude Code local workspace
.claude/